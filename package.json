{
    "name": "slaptax-mvp",
    "version": "0.1.0",
    "private": true,
    "description": "SLAPTAX MVP API server",
    "main": "api/server.js",
    "scripts": {
        "build": "npm --prefix web ci && npm --prefix web run build",
        "start": "node api/server.js",
        "dev": "node api/server.js",
        "test": "node --test api/tests"
    },
    "engines": {
        "node": ">=18"
    }
}
